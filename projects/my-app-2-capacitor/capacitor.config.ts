import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'myApp2.app',
  appName: 'my-app-2-capacitor',
  webDir: '../../dist/my-app-2/browser'
};

export default config;
