import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'myApp.app',
  appName: 'my-app-capacitor',
  webDir: '../../dist/my-app/browser'
};

export default config;
